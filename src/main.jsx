import React from 'react'
import ReactDOM from 'react-dom/client'
import PhotoGuestbook from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PhotoGuestbook />
  </React.StrictMode>
)
